global:
  scrape_interval: 15s # How frequently to scrape targets

scrape_configs:
  - job_name: "sdlc-agent"
    static_configs:
      - targets: ["localhost:5000"] # The address of your running Flask app